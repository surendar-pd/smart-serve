{
  "name": "shared-auth",
  "root": "libs/shared-auth",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "./gradlew assembleDebug"
      },
      "outputs": ["{projectRoot}/build"]
    }
  }
}
