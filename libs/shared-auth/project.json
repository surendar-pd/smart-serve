{
  "name": "shared-auth",
  "root": "libs/shared-auth",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "apps/provider-app/gradlew.bat -p libs/shared-auth assembleDebug"
      },
      "outputs": ["{projectRoot}/build"]
    }
  }
}
