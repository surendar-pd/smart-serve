package com.smartserve.customerapp.navigation

object Routes {
    /** Decides app vs auth from persisted Firebase session; not shown long. */
    const val Bootstrap = "bootstrap"
    const val Auth = "auth"
    const val App = "app"
}

